/*
#include "ble_central.h"

//send clock data
void ble_send_clock_frq( void ){

}


// send wake up from standby, is needed now
void ble_send_wake_up_sensor( void ){

}


//send go to standby, sensor in standby mode while no use for it
void ble_send_standby_sensor( void ){

}
*7